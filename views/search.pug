extends layouts/main
include mixins/videoBlock

block content
    .search__header
        h3 Searching for #{searchTerm}
    .search__videos
        if videos.length === 0
            h3 No Videos Found
        each item in videos
            +videoBlock(item)